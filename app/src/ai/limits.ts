// Shared by the writer prompt, schema refinements and the final quality gate.
export const POST_MIN_CHARACTERS = 700;
export const POST_TARGET_CHARACTERS = 850;
export const POST_MAX_CHARACTERS = 1000;
