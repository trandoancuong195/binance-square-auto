// Shared by the writer prompt, schema refinements and the final quality gate.
export const POST_MIN_CHARACTERS = 1200;
export const POST_TARGET_CHARACTERS = 1350;
export const POST_MAX_CHARACTERS = 3500;
